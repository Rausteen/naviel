---
export interface Props {
  href?: string;
  variant?: "primary" | "ghost";
  size?: "md" | "lg";
  class?: string;
  /** Ouvre dans un nouvel onglet (liens externes) */
  external?: boolean;
  type?: "button" | "submit";
  ariaLabel?: string;
  /** Ajoute une flèche → qui glisse au survol */
  arrow?: boolean;
}

const {
  href,
  variant = "primary",
  size = "md",
  class: className = "",
  external = false,
  type = "button",
  ariaLabel,
  arrow = false,
} = Astro.props;

const base =
  "group relative inline-flex items-center justify-center gap-2 rounded-[var(--radius-pill)] font-medium transition-all duration-300 ease-out focus-visible:outline-2 focus-visible:outline-offset-3 motion-reduce:transition-none";

const sizes = {
  md: "px-5 py-2.5 text-sm",
  lg: "px-7 py-3.5 text-base",
}[size];

const variants = {
  primary:
    "bg-accent text-[#10110a] hover:bg-[#d3f865] hover:-translate-y-0.5 hover:shadow-[0_12px_30px_-12px_rgba(200,242,78,0.55)] motion-reduce:hover:translate-y-0",
  ghost:
    "border border-line-2 text-ink hover:border-ink-soft hover:bg-white/[0.03] hover:-translate-y-0.5 motion-reduce:hover:translate-y-0",
}[variant];

const cls = [base, sizes, variants, className];
const externalAttrs = external
  ? { target: "_blank", rel: "noopener noreferrer" }
  : {};
---

{
  href ? (
    <a href={href} class:list={cls} aria-label={ariaLabel} {...externalAttrs}>
      <slot />
      {arrow && (
        <span
          class="transition-transform duration-300 group-hover:translate-x-1 motion-reduce:transition-none"
          aria-hidden="true"
        >
          →
        </span>
      )}
    </a>
  ) : (
    <button type={type} class:list={cls} aria-label={ariaLabel}>
      <slot />
      {arrow && (
        <span
          class="transition-transform duration-300 group-hover:translate-x-1 motion-reduce:transition-none"
          aria-hidden="true"
        >
          →
        </span>
      )}
    </button>
  )
}
